//import {} from '../actions/GBCActions'

const initialState = {
  board : {
    size : 20,
    data : []
  }
}

module.exports = function GameReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
